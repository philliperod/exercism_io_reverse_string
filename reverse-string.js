export const reverseString = (string) => {
  return string.split('').reverse().join('');
};

// 1) take the input and put it into an array
// 2) reverse the values in the array
// 3) join the reversed values in the array
